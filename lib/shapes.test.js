const {Circle, Square, Triangle} = require('./shapes')

describe('Circle test', () => {
    test('testing for a green background', () => {
        const Shape = new Circle()
        myShape.colorParam('green')
        expect
    })
})